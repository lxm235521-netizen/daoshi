# 单机应用部署（后端 + 前端）
COMPOSE_PROJECT_NAME=manju

# 对外端口
WEB_PORT=3002
API_PORT=5000

# 数据库连接串（必填）
# 口令含 @ 等保留字符时需百分号编码，例如 p@ssw0rd -> p%40ssw0rd
DATABASE_URL=postgresql://用户名:数据库密码@数据库服务器地址:5432/数据库名

# 生产环境务必替换为足够长的随机字符串
JWT_SECRET=change_this_to_a_long_random_jwt_secret
